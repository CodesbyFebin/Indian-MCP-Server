{
  "name": "@mcpserver/evidence",
  "version": "0.1.0",
  "description": "Evidence ledger and hash chain logic for MCPServer OS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --declaration --emitDeclarationOnly --outDir dist",
    "lint": "eslint src/**/*.ts --fix",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "eslint": "^8.0.0"
  }
}
