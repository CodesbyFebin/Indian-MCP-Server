{
  "name": "@mcpserver/security",
  "version": "0.1.0",
  "description": "Security utilities, secrets management, and isolation for MCPServer OS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --declaration --emitDeclarationOnly --outDir dist",
    "lint": "eslint src/**/*.ts --fix",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8",
    "node-vault": "^0.9.21"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "eslint": "^8.0.0"
  }
}
