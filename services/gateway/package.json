{
  "name": "@mcpserver-os/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "lint": "prettier --check src",
    "typecheck": "pnpm --filter @mcpserver-os/contracts build && tsc --noEmit"
  },
  "dependencies": {
    "@mcpserver-os/contracts": "workspace:*",
    "fastify": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.3"
  }
}
